# FastAPI and ASGI server
fastapi==0.104.1
uvicorn[standard]==0.24.0

# Pydantic & settings loader
pydantic
pydantic-settings

# Auth & security
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.0.1
python-multipart==0.0.6
email-validator
# Async MySQL support
sqlalchemy[asyncio]
asyncmy
geoalchemy2

# Alembic for migrations
alembic
pymysql

# Geospatial geometry
shapely

#seeding
faker

#Object storage (S3); the 'local' backend needs no extra packages
boto3>=1.34.0

# ML toxicity filter is optional - see requirements-ml.txt

#msg Encrypt imports
cryptography>=42.0


# 2FA Dependencies
pyotp>=2.8.0
qrcode[pil]>=7.4.2

#video session
